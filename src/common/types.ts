export type CheckBox = {
  id: number;
  value: boolean;
};

export type RamenStore = {
  name: string;
  reservation: string;
  waiting: string;
  tags: string;
};

export type ControlOption = {
  search: string;
  opening: string;
  eaten: string;
};
